apiVersion: v1
kind: Service
metadata:
  name: telemetry-daemon-svc
  namespace: gpu-scheduler
  labels:
    app: telemetry-daemon
spec:
  selector:
    app: telemetry-daemon
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
